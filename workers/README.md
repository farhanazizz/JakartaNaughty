# Workers

Experimental GPU workers. **Not** used by Coolify website deploy on `main`.

- `h3-comfyui/` — MiniMax H3 RunPod serverless worker (branch `feat/h3-runpod-worker` only until smoke-tested).
